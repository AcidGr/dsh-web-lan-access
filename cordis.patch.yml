# Bundle patch: mount the plugin row. The row name must match this package
# name; remove this insert block (or the bundle from the profile bundles
# list) to restore the original page exactly.
- insert:
    - id: lan-access
      name: 'dsh-web-lan-access'
